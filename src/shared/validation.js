/* =====================================
   VALIDATION.JS (CreditScore Pro)
   Placeholder minimal – implement rules later
   ===================================== */
export class FormValidator {
  constructor(config = {}) {
    this.config = config;
  }
  validate() {
    return true;
  }
}

// Expose globally if needed
window.FormValidator = FormValidator;

